(function(){
  const DATA='/data/products.json', INTEL='/data/product-intelligence.json';
  const state={products:[],intel:new Map(),selected:new Set(),brand:'',category:'',q:'',sort:'relevance'};
  const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const slug=s=>String(s||'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');
  function compareUrl(ids){const u=new URL('/compare/',location.origin);ids.slice(0,4).forEach(x=>u.searchParams.append('p',x));return u.pathname+u.search}
  function image(p){return p.image?`<img src="${esc(p.image)}" alt="${esc(p.name)}" loading="lazy">`:'<i class="fa-solid fa-mobile-screen-button"></i>'}
  function price(p){if(p.price!==undefined&&p.price!==null&&p.price!=='')return esc(p.price);const v=(p.variants||[]).find(x=>x.price);return v?esc(v.price):'Price not listed'}
  function intel(p){return state.intel.get(p.slug)||{}}
  function badges(i){let h='';if(i.has_price_drop)h+=`<span class="deal-badge">-${esc(i.price_drop_percent)}%</span>`;if(i.rank)h+=`<span class="rank-badge">#${esc(i.rank)}</span>`;return h}
  function sortList(list){return list.sort((a,b)=>{const x=intel(a),y=intel(b);if(state.sort==='deals')return (y.price_drop_percent||0)-(x.price_drop_percent||0);if(state.sort==='price'){const pa=x.best_verified_price||x.current_verified_price||Infinity,pb=y.best_verified_price||y.current_verified_price||Infinity;return pa-pb}if(state.sort==='verified')return String(y.last_verified||'').localeCompare(String(x.last_verified||''));return ((y.value_score??-1)-(x.value_score??-1))})}
  function mount(){
    const grid=document.getElementById('product-grid'),q=document.getElementById('product-search'),brand=document.getElementById('product-brand'),category=document.getElementById('product-category'),count=document.getElementById('product-count'),compare=document.getElementById('catalog-compare'),sort=document.getElementById('product-sort');
    if(!grid)return;
    const brands=[...new Set(state.products.map(p=>p.brand).filter(Boolean))].sort(),cats=[...new Set(state.products.map(p=>p.category).filter(Boolean))].sort();
    brand.innerHTML='<option value="">All brands</option>'+brands.map(x=>`<option>${esc(x)}</option>`).join('');category.innerHTML='<option value="">All categories</option>'+cats.map(x=>`<option>${esc(x)}</option>`).join('');
    function render(){
      const term=state.q.toLowerCase().trim();let list=state.products.filter(p=>(!state.brand||p.brand===state.brand)&&(!state.category||p.category===state.category)&&(!term||[p.name,p.brand,p.category,p.subcategory,p.summary].filter(Boolean).join(' ').toLowerCase().includes(term)));sortList(list);count.textContent=`${list.length} product${list.length===1?'':'s'}`;
      grid.innerHTML=list.length?list.map(p=>{const checked=state.selected.has(p.slug),i=intel(p),verified=i.last_verified?`Verified ${esc(i.last_verified)}`:'Verification pending',deal=i.has_price_drop?`<div class="catalog-deal"><strong>Best verified price: ${esc(i.best_verified_price)}</strong><span>Save ${esc(i.price_drop_amount)} (${esc(i.price_drop_percent)}%)</span></div>`:'';return `<article class="catalog-card"><a class="catalog-media" href="/products/${esc(p.slug)}/">${image(p)}<span class="catalog-badges">${badges(i)}</span></a><div class="catalog-body"><span class="catalog-kicker">${esc(p.brand||'')} · ${esc(p.category||'Gadget')}</span><h2><a href="/products/${esc(p.slug)}/">${esc(p.name)}</a></h2><strong class="catalog-price">${price(p)}</strong>${deal}<span class="catalog-availability">${esc(p.availability||'Availability not listed')} · ${verified}</span><div class="catalog-actions"><a href="/products/${esc(p.slug)}/">View details</a><button type="button" class="catalog-compare-btn ${checked?'selected':''}" data-slug="${esc(p.slug)}">${checked?'✓ Added':'Compare'}</button></div></div></article>`}).join(''):'<div class="catalog-empty"><i class="fa-solid fa-box-open"></i><h2>No catalog matches yet</h2><p>Search, brand, category and deal filters are ready. Add verified products to <code>data/products.json</code>.</p></div>';
      grid.querySelectorAll('.catalog-compare-btn').forEach(b=>b.onclick=()=>{const id=b.dataset.slug;if(state.selected.has(id))state.selected.delete(id);else if(state.selected.size<4)state.selected.add(id);else alert('You can compare up to 4 products.');render();updateCompare()});
    }
    function updateCompare(){const n=state.selected.size;compare.innerHTML=n?`<a class="catalog-floating-compare" href="${compareUrl([...state.selected])}"><i class="fa-solid fa-code-compare"></i> Compare ${n} product${n===1?'':'s'}</a>`:''}
    q?.addEventListener('input',e=>{state.q=e.target.value;render()});brand?.addEventListener('change',e=>{state.brand=e.target.value;render()});category?.addEventListener('change',e=>{state.category=e.target.value;render()});sort?.addEventListener('change',e=>{state.sort=e.target.value;render()});render();updateCompare();
  }
  Promise.all([fetch(DATA,{cache:'no-store'}).then(r=>r.json()),fetch(INTEL,{cache:'no-store'}).then(r=>r.json()).catch(()=>({products:[]}))]).then(([d,i])=>{state.products=(d.products||[]).map(p=>({...p,slug:p.slug||slug(p.name)}));(i.products||[]).forEach(x=>state.intel.set(x.slug,x));mount()}).catch(()=>{const g=document.getElementById('product-grid');if(g)g.innerHTML='<div class="catalog-empty"><h2>Product database unavailable</h2><p>Please try again after the catalog is built.</p></div>'});
})();
