(function(){const root=document.getElementById('compare-result'),picker=document.getElementById('compare-picker');let products=[];const params=new URLSearchParams(location.search);const selected=params.getAll('p').concat([params.get('a'),params.get('b')]).filter(Boolean);const unique=a=>[...new Set(a)].slice(0,4);function esc(s){return String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}function render(){const ids=unique(selected);picker.innerHTML='<label>Add products <select id="product-select"><option value="">Choose a product…</option>'+products.filter(p=>!ids.includes(p.slug)).map(p=>`<option value="${esc(p.slug)}">${esc(p.name)}</option>`).join('')+'</select></label><span class="compare-count">${ids.length}/4 selected</span>';document.getElementById('product-select').onchange=e=>{if(e.target.value){const u=new URL(location);u.searchParams.append('p',e.target.value);location.href=u}};const chosen=ids.map(id=>products.find(p=>p.slug===id)).filter(Boolean);if(!chosen.length){root.innerHTML='<div class="compare-empty"><i class="fa-solid fa-code-compare"></i><h2>Choose products to compare</h2><p>Add up to four products. Only information present in the product catalog is shown.</p></div>';return}const keys=[...new Set(chosen.flatMap(p=>Object.keys(p.specifications||{})))];root.innerHTML='<div class="compare-table-wrap"><table class="compare-table"><thead><tr><th>Specification</th>'+chosen.map(p=>`<th><a href="/products/${esc(p.slug)}/">${esc(p.name)}</a><button class="remove-product" data-id="${esc(p.slug)}" title="Remove">×</button></th>`).join('')+'</tr></thead><tbody><tr><td>Brand</td>'+chosen.map(p=>`<td>${esc(p.brand||'—')}</td>`).join('')+'</tr><tr><td>Category</td>'+chosen.map(p=>`<td>${esc(p.category||'—')}</td>`).join('')+'</tr><tr><td>Price</td>'+chosen.map(p=>`<td><strong>${esc(p.price||'Not listed')}</strong></td>`).join('')+'</tr>'+keys.map(k=>`<tr><td>${esc(k)}</td>`+chosen.map(p=>`<td>${esc((p.specifications||{})[k]||'—')}</td>`).join('')+'</tr>').join('')+'</tbody></table></div><p class="compare-note">Comparison highlights only catalog data. No missing specification is guessed.</p>';document.querySelectorAll('.remove-product').forEach(b=>b.onclick=()=>{const u=new URL(location);u.searchParams.delete('p');const keep=unique(selected).filter(x=>x!==b.dataset.id);u.search='';keep.forEach(x=>u.searchParams.append('p',x));location.href=u})}fetch('/data/products.json').then(r=>r.json()).then(d=>{products=d.products||[];render()}).catch(()=>{root.innerHTML='<div class="compare-empty"><h2>Product catalog unavailable</h2><p>Please try again after the catalog is built.</p></div>'})})();