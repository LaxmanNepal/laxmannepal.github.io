# Homepage checks

Run `python scripts/check-homepage-integrity.py` from the repository root.

The check ensures the homepage has exactly one `site-enhancements.js` renderer, does not load the obsolete `apps-renderer.js`, and has both generated data files.